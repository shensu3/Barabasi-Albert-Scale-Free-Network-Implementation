package test;
/*class to stope ip address and port numbers to feed it to the 
 * probability based unique selection algorithm to select
 * ip-port pairs based on degree
 */
public class listMem { 
	    public String IPAddress;
	    public int    ipport; 
	    public listMem(String IPaddress,int ipport)
	    {
	    	IPAddress=IPaddress;
	    	this.ipport=ipport;
	    }

}
